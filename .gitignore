node_modules/
dist/
release/
coverage/
*.log
.DS_Store
.vite/

# Artefato temporário gerado pelo Vitest ao carregar a config
*.timestamp-*.mjs

# Env local de dev/QA (o template versionado é `.env.example`)
.env
.env.local
.env.*.local

# Fontes brutas de arte/áudio (packs originais, não recortados). Só os
# derivados efetivamente usados são versionados em src/assets/.
/art-sources/
*.zip
*.rar
.vercel
